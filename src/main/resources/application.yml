#spring:
#  datasource:
#    url: jdbc:h2:tcp://localhost/~/jpashop # MVCC=TRUE 는 여러개가 접근할 떄 속도 빠르게
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create # 자동으로 테이블 만들어줌
#    properties:
#      hibernate:
#        show_sql: true   # System.out을 통해서 출력
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug  # log를 통해서 출력
  org.hibernate.type: trace
